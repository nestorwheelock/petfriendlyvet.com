"""Browser-based E2E tests using Playwright.

These tests require Playwright to be installed:
    pip install pytest-playwright playwright
    playwright install

Run browser tests:
    pytest tests/e2e/browser/ -m browser

Skip browser tests:
    pytest -m "not browser"
"""
